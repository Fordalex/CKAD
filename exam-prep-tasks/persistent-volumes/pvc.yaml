apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
  namespace: database
spec:
  accessModes:
    # TODO: Add access mode
  storageClassName: manual
  resources:
    requests:
      storage: # TODO: Add storage request
